const p5Docs = {
    createCanvas: "createCanvas(w, h) → sets up the drawing canvas.",
    background:   "background(r, g, b) → sets the background color.",
    line:         "line(x1, y1, x2, y2) → draws a line segment.",
    noise:        "noise(x, [y], [z]) → perlin noise value.",
    // ...add more, or auto-generate from the p5 site later
  };
  